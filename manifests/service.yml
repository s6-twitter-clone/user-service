apiVersion: v1
kind: Service
metadata:
    name: "twiddit-810e"
    labels:
        app: "twiddit-810e"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twiddit-810e"