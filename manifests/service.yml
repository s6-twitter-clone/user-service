apiVersion: v1
kind: Service
metadata:
    name: "twidditcluster-7cb2"
    labels:
        app: "twidditcluster-7cb2"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twidditcluster-7cb2"