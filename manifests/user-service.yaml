# deployment
#
# set up a deployment for the user API.
apiVersion : apps/v1
kind: Deployment
metadata:
  name: "twidditcluster-7cb2"
spec:
  replicas: 2
  selector:
    matchLabels:
      app: "twidditcluster-7cb2"
  template:
    metadata:
      labels:
        app: "twidditcluster-7cb2"
    spec:
      containers:
        - name: "twidditcluster-7cb2"
          image: "twiddit.azurecr.io/twidditcluster"
          ports:
          - containerPort: 80

---
# service

apiVersion: v1
kind: Service
metadata:
    name: "twidditcluster-7cb2"
    labels:
        app: "twidditcluster-7cb2"
spec:
    type: ClusterIP
    ports:
    - port: 80
      targetPort: 80
      protocol: TCP
      name: http
    selector:
        app: "twidditcluster-7cb2"

---
# ingress
#
# Sets up an ingress to route traffic to the user service

apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
    name: "twidditcluster-7cb2"
    labels:
        app: "twidditcluster-7cb2"
    annotations:
        kubernetes.io/ingress.class: addon-http-application-routing
        ingress.kubernetes.io/ssl-redirect: "false"
        nginx.ingress.kubernetes.io/rewrite-target: /
spec:
    rules:
        - host: default-twidditcluster-7cb2.bdc33b62f0d9413db518.uksouth.aksapp.io
          http:
            paths:
            - path: /api/users
              pathType: Prefix
              backend:
                service:
                    name: "twidditcluster-7cb2"
                    port: 80